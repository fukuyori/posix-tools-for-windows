# less - GNU less互換のページャー（Windows向け）

Rustで実装したGNU less互換のテキストファイル閲覧ツールです。Windows上でもLinux同様のPOSIX準拠の操作性を実現しています。

## 特徴

- **複数エンコーディング対応**：UTF-8、Shift_JIS、EUC-JP、ISO-2022-JP を自動判定
- **glob展開対応**：ワイルドカード（`*`, `?`）で複数ファイル指定可能
- **ターミナルサイズ変更対応**：リアルタイムで画面をリサイズ
- **Windows最適化**：Windows標準の大文字小文字を区別しないファイル操作に対応
- **軽量・高速**：Rust実装で高速起動・低メモリ消費
- **Unix互換**：less の各種オプション・キーバインディングに対応

## インストール

### 事前要件
- Rust 1.70 以上

### ビルド

```bash
cargo build --release
```

実行ファイルは `target/release/less.exe` に生成されます。

## 使用方法

```bash
less [オプション] [ファイル]...
```

### 基本的な使用例

```bash
# 単一ファイルの表示
less myfile.txt

# 複数ファイルの表示
less *.txt

# パイプでの使用
dir | less

# 行番号を表示して起動
less -N myfile.txt

# 特定行から表示
less +100 myfile.txt

# 特定パターンを検索して表示
less +/pattern myfile.txt

# tail -f モードで起動
less +F myfile.txt
```

## オプション

### 表示オプション
| オプション | 機能 |
|-----------|------|
| `-N`, `--line-numbers` | 行番号を表示 |
| `-S`, `--chop-long-lines` | 長い行を折り返さない（横スクロール可能） |
| `-s`, `--squeeze-blank-lines` | 連続する空行を1行に圧縮 |
| `-x N`, `--tabs=N` | タブ幅を設定（デフォルト: 8） |

### 検索・フィルタオプション
| オプション | 機能 |
|-----------|------|
| `-i`, `--ignore-case` | 検索で大文字小文字を区別しない |
| `-I`, `--IGNORE-CASE` | スマートケース（大文字含むパターンは区別） |
| `-p pattern` | 起動時に検索 |

### 動作オプション
| オプション | 機能 |
|-----------|------|
| `-F`, `--quit-if-one-screen` | 1画面に収まれば即終了 |
| `-e`, `--quit-at-eof` | ファイル末尾で終了 |
| `-X`, `--no-init` | 終了時に画面をクリアしない |
| `-m` | プロンプトにパーセントを表示 |
| `-M` | 詳細なプロンプトを表示 |

### スタートアップオプション
| オプション | 機能 |
|-----------|------|
| `+N` | N行目から表示 |
| `+/pattern` | 起動時にpatternを検索 |
| `+F` | tail -f モードで起動 |

## キー操作

### 移動

| キー | 機能 |
|------|------|
| `j`, `↓`, `Enter`, `e` | 1行下へ |
| `k`, `↑`, `y` | 1行上へ |
| `f`, `Space`, `PgDn` | 1ページ下へ |
| `b`, `PgUp` | 1ページ上へ |
| `d` | 半ページ下へ |
| `u` | 半ページ上へ |
| `g`, `Home` | 先頭へ |
| `G`, `End` | 末尾へ |
| `Ng` | N行目へ（例: `100g`） |
| `→`, `l` | 右へスクロール（`-S` 時） |
| `←`, `h` | 左へスクロール（`-S` 時） |

### 検索

| キー | 機能 |
|------|------|
| `/pattern` | 前方検索 |
| `?pattern` | 後方検索 |
| `n` | 次の検索結果 |
| `N` | 前の検索結果 |
| `&pattern` | パターンにマッチする行のみ表示（フィルタ） |

### その他

| キー | 機能 |
|------|------|
| `=` | ファイル情報を表示 |
| `v` | エディタで開く |
| `F` | tail -f モード（Ctrl+C で戻る） |
| `q`, `Q` | 終了 |

## 環境変数

| 変数 | 説明 |
|------|------|
| `VISUAL` | `v` キーで起動するエディタ（優先度高） |
| `EDITOR` | `v` キーで起動するエディタ（優先度低） |

設定されていない場合は `notepad` (Windows標準のメモ帳) が使用されます。

## 依存関係

- **crossterm**: ターミナル制御
- **encoding_rs**: 文字コード自動判定
- **glob**: ファイルパターンマッチング
- **unicode-width**: Unicode文字幅計算

## 実装の特徴

### Windows互換性
- backslash (`\`) パスとforwardslash (`/`) パスの両方に対応
- 大文字小文字を区別しないglob展開
- Windows標準のLFとCRLFの自動判定

### POSIX互換性
- `*` と `?` によるglob展開対応
- Unix由来のless互換キーバインディング
- 標準入力からのパイプ処理対応

### 文字エンコーディング対応
自動判定によるエンコーディング検出：
1. UTF-8 BOM の確認
2. ISO-2022-JP の特徴バイト列の確認
3. UTF-8 の妥当性確認
4. Shift_JIS と EUC-JP のスコア比較

## トラブルシューティング

### 文字化けが発生する場合
エンコーディングの自動判定に失敗している可能性があります。
ファイルのエンコーディングを確認してください。

### 検索結果が見つからない場合
- 大文字小文字の違いを確認
- `-i` オプション（大文字小文字を区別しない）を試す
- 正規表現ではなく、リテラル文字列での検索です

### ターミナルサイズの不具合
ターミナルの大きさを変更してください。
自動的に適応してきちんと表示されます。

## ライセンス

MIT

## 作者

Claude (Rust実装)

## 参考

- GNU less: https://www.greenwoodsoftware.com/less/
